"use client";

import * as React from "react";
import { useParams } from "next/navigation";
import { Activity, Save, ShieldAlert } from "lucide-react";
import { PrivateShell } from "@/components/templates/private-shell";
import { getReturnToActivity, saveReturnToActivity, type ReturnToActivityPlan } from "@/shared/lib/api";
import { usePreferredLocale } from "@/shared/lib/locale";

const stages = [["Daily activities", "Atividades diárias"], ["Light physical activity", "Atividade física leve"], ["Non-contact activity", "Atividade sem contacto"], ["Higher-intensity activity", "Atividade de maior intensidade"], ["Contact / high-risk activity", "Atividade de contacto / alto risco"]];

export default function ReturnToActivityPage() {
  const params = useParams();
  const locale = usePreferredLocale("private", (params?.locale as string) || "en");
  const isPt = locale === "pt";
  const [plan, setPlan] = React.useState<ReturnToActivityPlan>({ currentStage: 1, activityType: "", durationMinutes: 0, intensity: "", symptomsBefore: "", symptomsAfter: "", notes: "" });
  const [loading, setLoading] = React.useState(true);
  const [message, setMessage] = React.useState("");
  React.useEffect(() => { const timeout = window.setTimeout(() => { getReturnToActivity().then((value) => { if (value) setPlan(value); }).finally(() => setLoading(false)); }, 0); return () => window.clearTimeout(timeout); }, []);
  const update = (key: keyof ReturnToActivityPlan, value: string | number) => setPlan((current) => ({ ...current, [key]: value }));
  async function submit(event: React.FormEvent) { event.preventDefault(); setMessage(""); await saveReturnToActivity(plan); setMessage(isPt ? "Registo guardado." : "Tracking information saved."); }
  return <PrivateShell><main className="return-page"><span className="return-kicker">NOVA RECOVERY</span><h1>{isPt ? "Regresso à atividade" : "Return to Activity"}</h1><p className="return-lead">{isPt ? "Regista atividades e sintomas sem transformar os dados em autorização médica." : "Track activities and symptoms without turning the data into medical clearance."}</p><div className="return-notice"><ShieldAlert size={20}/><span>{isPt ? "Para contacto, colisão, quedas ou atividades de alto risco, a decisão deve ser feita com supervisão e autorização profissional apropriadas." : "For contact, collision, fall, or high-risk activities, progression decisions require appropriate professional supervision and clearance."}</span></div>{loading ? <p>{isPt ? "A carregar..." : "Loading..."}</p> : <form onSubmit={submit}><section className="return-card"><div className="return-title"><Activity size={21}/><h2>{isPt ? "Progressão informativa" : "Information-only progression"}</h2></div><div className="stage-list">{stages.map((stage, index) => <label key={stage[0]} className={plan.currentStage === index + 1 ? "stage active" : "stage"}><input type="radio" name="stage" checked={plan.currentStage === index + 1} onChange={() => update("currentStage", index + 1)}/><span className="stage-number">0{index + 1}</span><strong>{isPt ? stage[1] : stage[0]}</strong></label>)}</div></section><section className="return-card form-grid"><label>{isPt ? "Tipo de atividade" : "Activity type"}<input value={plan.activityType ?? ""} onChange={(event) => update("activityType", event.target.value)} placeholder={isPt ? "Ex.: caminhada" : "e.g. walking"}/></label><label>{isPt ? "Duração (minutos)" : "Duration (minutes)"}<input type="number" min="0" max="1440" value={plan.durationMinutes ?? 0} onChange={(event) => update("durationMinutes", Number(event.target.value))}/></label><label>{isPt ? "Intensidade/categoria" : "Intensity/category"}<input value={plan.intensity ?? ""} onChange={(event) => update("intensity", event.target.value)} placeholder={isPt ? "Ex.: leve" : "e.g. light"}/></label><label>{isPt ? "Sintomas antes" : "Symptoms before"}<textarea value={plan.symptomsBefore ?? ""} onChange={(event) => update("symptomsBefore", event.target.value)}/></label><label>{isPt ? "Sintomas depois" : "Symptoms after"}<textarea value={plan.symptomsAfter ?? ""} onChange={(event) => update("symptomsAfter", event.target.value)}/></label><label className="full">{isPt ? "Notas" : "Notes"}<textarea value={plan.notes ?? ""} onChange={(event) => update("notes", event.target.value)}/></label><button className="return-primary" type="submit"><Save size={16}/>{isPt ? "Guardar registo" : "Save tracking"}</button></section></form>}<p className="return-footnote">{isPt ? "A NOVA não determina prontidão, progressão ou medical clearance." : "NOVA does not determine readiness, progression, or medical clearance."}</p>{message && <p role="status" className="return-message">{message}</p>}</main><style jsx>{`.return-page{max-width:900px;margin:0 auto;padding:12px 20px 48px}.return-kicker{color:#00d2b5;font-size:.7rem;font-weight:800;letter-spacing:.14em}.return-page h1{margin:8px 0}.return-lead{color:rgba(255,255,255,.65);margin:0 0 22px}.return-notice{display:flex;gap:10px;padding:14px 16px;border:1px solid rgba(245,158,11,.35);background:rgba(245,158,11,.08);color:#fde68a;border-radius:10px;line-height:1.5;font-size:.82rem;margin-bottom:18px}.return-card{background:#0a0e1a;border:1px solid rgba(255,255,255,.1);border-radius:12px;padding:22px;margin-bottom:16px}.return-title{display:flex;gap:10px;align-items:center}.return-title svg{color:#00d2b5}.return-title h2{font-size:1.1rem;margin:0}.stage-list{display:grid;gap:8px;margin-top:18px}.stage{display:flex;align-items:center;gap:14px;padding:12px;border:1px solid rgba(255,255,255,.1);border-radius:9px;color:rgba(255,255,255,.7);cursor:pointer}.stage input{accent-color:#00d2b5;width:auto}.stage.active{border-color:#00d2b5;background:rgba(0,210,181,.08);color:#fff}.stage-number{color:#00d2b5;font-weight:800}.form-grid{display:grid;grid-template-columns:1fr 1fr;gap:16px}.form-grid label{display:grid;gap:7px;color:rgba(255,255,255,.75);font-size:.82rem}.form-grid input,.form-grid textarea{width:100%;box-sizing:border-box;padding:11px;border-radius:8px;border:1px solid rgba(255,255,255,.14);background:rgba(255,255,255,.05);color:#fff}.form-grid textarea{min-height:72px;resize:vertical}.form-grid .full{grid-column:1/-1}.return-primary{display:inline-flex;align-items:center;gap:8px;border:0;border-radius:8px;padding:12px 16px;background:#00d2b5;color:#061018;font-weight:800;cursor:pointer}.return-footnote,.return-message{color:rgba(255,255,255,.58);font-size:.8rem}.return-message{color:#00d2b5}@media(max-width:650px){.form-grid{grid-template-columns:1fr}.form-grid .full{grid-column:auto}}`}</style></PrivateShell>;
}
